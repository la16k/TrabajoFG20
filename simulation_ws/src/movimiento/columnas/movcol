#include <stdio.h>
#include <math.h>
#include <stdlib.h>

#define LIMABAJO = -3.30 # Limite eje y por abajo
#define LIMARRIBA = 1.82 # Limite eje y por abajo

class columna {
    private: 
    int tipo;
    int posicionini[2];

    public: 
    int get_tipo(){return tipo;};
    int get_posicionini(){return *posicionini;};
    void movimiento(int tipo, int *posicionini);
};

void columna::movimiento(int tipo, int *posicionini) {
    int i;
    for (i = 1; i<5; i++)
        if (tipo == i) {

        }

}
